node_modules/
dist/
coverage/

# local env (secrets/config) — keep the example
.env*
!.env.example

# Firebase service-account keys — secret, never commit
serviceAccount.json
service.json
*-firebase-adminsdk-*.json

.DS_Store
*.log
